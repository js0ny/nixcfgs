## Nix scripting notes

- In shell snippets generated by Nix, prefer `lib.getExe`/`lib.getExe'` for binaries (for example `lib.getExe pkgs.socat`) instead of hardcoded store paths like `${pkgs.socat}/bin/socat`.
